name: Test

on:
  push:
    branches:
      - '**'
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - 'docs/**'
  pull_request:
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - 'docs/**'

permissions:
  contents: read

jobs:
  test:
    name: Test (linux-amd64)
    runs-on: ubuntu-latest
    env:
      CGO_ENABLED: '0'

    steps:
      - uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: stable

      - name: Build
        run: |
          go build -v -trimpath -ldflags '-s -w' ./cmd/shadowsocks-go
          go build -v -trimpath -ldflags '-s -w' ./cmd/shadowsocks-go-domain-set-converter

      - name: Build Flysky ssbad
        run: go build -v -trimpath -ldflags '-s -w' ./cmd/sstest

      - name: Ensure Flysky ssbad does not link the MySQL panel
        shell: bash
        run: |
          if go list -deps ./cmd/sstest | grep -E 'go-sql-driver/mysql|internal/panel'; then
            echo "Flysky ssbad must not link MySQL or the legacy panel adapter"
            exit 1
          fi

      - name: Test
        run: go test -v ./...

      - name: Upload Binaries
        uses: actions/upload-artifact@v7
        with:
          name: ssbad-${{ github.sha }}-linux-${{ runner.arch }}
          path: |
            shadowsocks-go*
            sstest
